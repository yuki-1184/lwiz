import type { Meta, StoryObj } from '@storybook/react';

import { ResetPasswordForm } from '.';

export default {
  component: ResetPasswordForm,
} satisfies Meta<typeof ResetPasswordForm>;

export const Primary: StoryObj<typeof ResetPasswordForm> = {};
